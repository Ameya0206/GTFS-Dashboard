# GTFS Dashboard — backend Python dependencies
#
# Pin minor versions for reproducibility but leave patch versions open so
# security fixes are picked up automatically.
#
# Install with:  pip install -r requirements.txt

# Web framework
fastapi>=0.111,<0.200
uvicorn[standard]>=0.29,<0.40      # ASGI server; [standard] includes watchfiles for --reload

# Data handling
pandas>=2.2,<3.0
requests>=2.31,<3.0                # Used by loader.py for URL ingestion

# Validation / serialisation (FastAPI depends on pydantic v2 by default)
pydantic>=2.7,<3.0

# Multipart form handling (required for file uploads in FastAPI)
python-multipart>=0.0.9,<1.0

# Optional but recommended for dev
httpx>=0.27,<1.0                   # Async HTTP client; useful for testing FastAPI with TestClient
